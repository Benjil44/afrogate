{
  "name": "afrows",
  "version": "0.114.28",
  "private": true,
  "description": "Afrows operations, monitoring, routing, and billing platform.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "node scripts/dev-all.mjs",
    "dev:backend": "npm --workspace @afrows/backend run dev",
    "dev:dashboard": "npm --workspace @afrows/dashboard run dev",
    "dev:client": "npm --workspace @afrows/client run dev",
    "agent:dev": "python apps/agent/run.py --once",
    "db:setup:local": "powershell -ExecutionPolicy Bypass -File scripts/setup-local-postgres.ps1",
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "contrast:check": "node scripts/check-dashboard-contrast.mjs",
    "secrets:check": "node scripts/check-secrets.mjs",
    "test:backend": "npm --workspace @afrows/backend run test",
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed",
    "version:patch": "node scripts/bump-version.mjs patch",
    "version:minor": "node scripts/bump-version.mjs minor",
    "version:major": "node scripts/bump-version.mjs major",
    "version:set": "node scripts/bump-version.mjs",
    "version:check": "node plugins/afrows-versioning/scripts/check-version.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0"
  }
}
