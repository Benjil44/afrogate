# Docker Notes

Docker is intentionally not required for the first local MVP. Keep this folder for future Compose files when deployment needs to become more reproducible.

Expected future services:

- backend
- dashboard
- postgres
- redis

