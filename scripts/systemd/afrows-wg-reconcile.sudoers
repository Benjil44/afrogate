# Allow the unprivileged backend (user: afrows) to trigger ONE command: an
# immediate run of the WireGuard reconciler. Install as /etc/sudoers.d/afrows-wg
# with mode 0440 (validate with `visudo -cf`).
afrows ALL=(root) NOPASSWD: /usr/bin/systemctl start afrows-wg-reconcile.service
