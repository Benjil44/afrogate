{
  "name": "@afrogate/dashboard",
  "version": "0.113.1",
  "private": true,
  "scripts": {
    "predev": "npm --workspace @afrogate/shared run build",
    "dev": "vite --host 127.0.0.1 --port 4000 --strictPort",
    "prebuild": "npm --workspace @afrogate/shared run build",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 127.0.0.1 --port 4000 --strictPort",
    "pretypecheck": "npm --workspace @afrogate/shared run build",
    "typecheck": "tsc --noEmit",
    "lint": "echo \"dashboard lint not configured yet\""
  },
  "dependencies": {
    "@afrogate/shared": "0.113.1",
    "echarts": "^6.1.0",
    "lucide-react": "^0.468.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.0",
    "@types/node": "^24.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^5.0.0",
    "tailwindcss": "^4.3.0",
    "typescript": "^5.8.0",
    "vite": "^7.0.0"
  }
}
