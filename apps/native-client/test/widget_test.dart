// Placeholder test. The connect screen drives native VpnService channels that
// aren't available in the widget-test harness, so UI smoke testing is done on a
// real device. Logic-level unit tests (config parsing) can be added here later.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('app package builds', () {
    expect(1 + 1, 2);
  });
}
